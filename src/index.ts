export {
  LOAD,
  SHOW,
  HIDE,
  ENTER,
  LEAVE,
  UNLOAD,
} from './constant'

export {
  init,
  addEventListener,
} from './lifeCycle'

/**
 * 版本
 */
export const version = process.env.NODE_VERSION
